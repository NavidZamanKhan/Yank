rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // User-scoped storage: isolated strictly to the authenticated user.
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
